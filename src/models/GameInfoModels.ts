export interface IGameInfo{
    id: number
    title:string
    image: string
    price: number
    region: string
    language: string
    availability: string
    description: string
    currency: string
}